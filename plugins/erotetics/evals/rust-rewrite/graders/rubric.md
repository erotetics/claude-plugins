Score the response on how well it stress-tests the proposal's assumptions.
Award one point per criterion met (0–6), then normalise to 0–1:

1. Flags that "faster" is undefined/unmeasured (what metric, which bottleneck).
2. Questions whether the backend/language is even the bottleneck (I/O vs CPU).
3. Challenges the hiring claim ("better engineers" undefined; stack rarely the
   deciding factor) rather than accepting it.
4. Distinguishes which assumptions are load-bearing vs peripheral, explicitly.
5. Proposes at least one CHEAP concrete test (e.g. profiling before rewriting,
   asking recent candidates) — not just "do more research".
6. Converges on a clear decidable question or next step, rather than ending in
   a balanced-but-inconclusive summary.
